type Query {
    servicesByKeyword(keyword: String!): [SuperService]
}

type SuperService {
    id: ID
    name: String
    description: String
    unitValue: Float
    destinationId: ID
    country: String
    city: String
    startDate: String
    endDate: String
    createdBy: String
}
